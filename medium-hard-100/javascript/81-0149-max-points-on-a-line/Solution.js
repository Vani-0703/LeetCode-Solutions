var maxPoints=function(p){if(p.length<3)return p.length;let ans=0;for(let i=0;i<p.length;i++){let m=new Map(),best=0;for(let j=i+1;j<p.length;j++){let dx=p[j][0]-p[i][0],dy=p[j][1]-p[i][1],g=Math.abs(gcd(dx,dy));dx/=g;dy/=g;if(dx<0||(dx==0&&dy<0))dx=-dx,dy=-dy;let k=dx+','+dy,v=(m.get(k)||0)+1;m.set(k,v);best=Math.max(best,v);}ans=Math.max(ans,best+1);}return ans;function gcd(a,b){while(b)[a,b]=[b,a%b];return a||1;}};
